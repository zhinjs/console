# demo.zhin.dev Console 构建（复制为 .env.demo 后按需修改）
# 用法：set -a && source .env.demo && set +a && pnpm build:demo

VITE_DEMO_MODE=1
VITE_API_BASE=https://zhinjs-demo.hf.space
VITE_API_TOKEN=zhin-demo

# 本地联调本机 Host：
# VITE_API_BASE=http://127.0.0.1:8086
# VITE_API_TOKEN=demo-public-token-change-me
